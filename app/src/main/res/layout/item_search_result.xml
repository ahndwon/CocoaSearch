<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/tools">

    <data>

        <variable
            name="document"
            type="xyz.thingapps.cocoasearch.vo.Document" />

    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/card_view_margin"
        app:cardCornerRadius="16dp">

        <ImageView
            android:id="@+id/searchImageView"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:contentDescription="@string/description_search_results"
            app:loadImageWithCenterCrop="@{document.imageUrl}"
            bind:itemHeight="@{document.height}"
            bind:itemWidth="@{document.width}" />

    </androidx.cardview.widget.CardView>

</layout>
